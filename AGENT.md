# 중요 !!

**!!! 항상 각 패키지에는 REAMD.md를 생성하여 각 노드 혹은 런치의 실행방법과 기능과 그리고 토픽들을 간단하게 요약해서 정리하기 !!!**

## ⚓ agent.md: [Port-ER] 세맨틱 슬램 및 멀티모달 에이전트 기반 스마트 항만 관제 시스템 명세서

본 문서는 5090 인프라 서버(AI/대뇌)와 **게이밍 노트북(ROS2/하드웨어 제어)** 간의 분산 컴퓨팅 환경을 기반으로 구축되는 스마트 항만 멀티 AMR 자율 관제 플랫폼의 전체 시스템 명세서이다.

## 1. 프로젝트 아키텍처 개요 (System Architecture)

본 프로젝트는 무거운 생성형 AI(VLM/LLM) 모델이 물리 하드웨어를 직접 실시간 제어할 때 발생하는 지연 시간(Latency)과 불확실성(Hallucination)을 극복하기 위해, 고차원 행정/태스크 스케줄링(High-level Planning)과 저차원 기하학 실행/안전 보장(Low-level Execution)을 분리한 하이브리드 가상 물리 시스템(CPS) 구조를 채택한다.

```text
┌────────────────────────────────────────────────────────┐
│               5090 인프라 서버 (AI 브레인 영역)             │
│  - YOLOv11 Multi-task (Instance Segmentation + OBB)    │
│  - n8n Workflow 플랫폼 (실시간 CSV 기록 및 데이터 행정)       │
│  - 로컬 LLM & RAG Vector DB (관리자 브리핑 생성기)          │
└──────────────────────────▲─────────────────────────────┘
                           │ (HTTP Webhook / JSON 통신)
                           ▼
┌────────────────────────────────────────────────────────┐
│           게이밍 노트북 (중앙 관제탑 및 ROS2 브릿지)          │
│  - n8n 명령 수신 및 ROS2 액션/서비스 전환 매핑                │
│  - [안전장치] 0.01초 컷 로컬 비상 정지 인터럽트 구동             │
└───────┬──────────────────┬──────────────────┬──────────┘
        │ (ROS2 Action)    │ (ROS2 Service)   │ (ROS2 Topic)
        ▼                  ▼                  ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│  차량 (AMR)   │   │ 로봇팔 크레인   │   │   카메라 인식  │
│              │   │              │   │              │
└──────────────┘   └──────────────┘   └──────────────┘
```

## 2. 파트별 상세 명세 및 핵심 팩트 (Component Specifications)

### 👁️ A. 비전 인프라 파트 (로컬 서버 구동)

탑다운 전체 감시 카메라의 단일 영상 스트림을 활용해 항만 내부의 거시적 맥락을 완벽히 디지털 트윈화한다. 차량 커스텀 학습(Fine-tuning)에 소요되는 리소스를 과감히 생략하고 순정 YOLOv11 가속 엔진과 아루코 마커를 융합한다.

- **공간 인지 (Instance Segmentation):** 주행 차선, 야적장 슬롯(Slot_A, B, C), 선박 선석 구역을 픽셀 단위 폴리곤으로 따내어 차량 및 장애물의 구역 점유(Occupancy) 상태를 연산한다.
- **객체 인지 (Instance Segmentation):** 대각선 주행 및 회전 시 발생하는 사각형 박스 간의 간섭과 ID 뒤바뀜 에러를 방지하기 위해 사물의 회전 각도($\theta$)까지 완벽히 검출하는 회전 바운딩 박스를 사용한다.
- **글로벌 위치 추적:** 차량 지붕에 장착된 대형 아루코(ArUco) 보드 고유 번호를 통해 차량의 글로벌 ID와 실시간 좌표를 $3D$ 호모그래피 매트릭스로 역추적한다.

### 🤖 B. 하드웨어 제어 파트 (게이밍 노트북 구동)

관제탑의 명령에 따라 차량과 로봇팔이 오차 없이 물리 세계의 임무를 완수하도록 제어 프로토콜을 규격화한다.

- **차량 제어 (AMR):** 라이다(LiDAR) 및 IMU 센서 기반의 NDT SLAM 지도를 독자 구동하며, ROS2 Nav2 액션 클라이언트를 통해 지정된 구역 목표점까지 장애물을 회전 회피하며 자율주행한다.
- **로봇팔 제어 (Crane):** 정밀 적재가 필요한 선적/하역 시에는 AI 추론을 차단하고 로봇팔 손단 캠의 **OpenCV ArUco Detector** 기하학 연산으로 즉시 전환한다. 마커 테두리 픽셀 기준 상자의 상대 변환 행렬을 계산하여 밀리미터($mm$) 단위의 2단 고적(Stacking) 및 빈틈없는 정렬(테트리스)을 기계적으로 마감한다.

### 🧠 C. 중앙 관제 및 데이터 행정 파트 (n8n 워크플로우 구동)

데이터 행정의 신뢰성과 인간 친화적 관제를 동시에 달성하기 위해 **CSV 다이렉트 컨텍스트 주입**과 **RAG 지식 매칭 기술**을 분리 구동한다.

1. **실시간 데이터 로그 영구화:** 물리 이벤트 수신 즉시 `port_inventory_log.csv`에 자재 이력(`Timestamp, Target_ID, Location_Zone, Status`)을 누적 기록한다.
2. **RAG 지식 베이스 아카이빙:** ChromaDB 벡터 저장소에 미리 '전문가용 관제사 브리핑 템플릿(정상 이송, 핸드오버 간섭, 얀-샤플 발생 등)' 풀을 대량 임베딩해 둔다.
3. **하이브리드 출력 프롬프트 엔진:** n8n 내부에서 실시간 CSV 텍스트와 RAG 검색 결과를 조합하여 로컬 LLM에 주입, [하드웨어 제어용 JSON]과 [관리자 대시보드 표시용 자연어 브리핑 문장]을 듀얼 트랙으로 동시에 뿜어낸다.

## 3. 중앙 관제 인터페이스 데이터 규격 (Data Specification)

### ① 입력 (노트북 ROS2 브릿지 ──> n8n 웹훅)

JSON

```json
{
  "event_type": "ZONE_CHANGED",
  "target_id": "AMR_B",
  "current_zone": "Lane_1",
  "status": "MOVING"
}
```

### ② 출력 (n8n 에이전트 ──> 노트북 ROS2 브릿지)

JSON

```json
{
  "control_data": {
    "command": "DISPATCH",
    "target": "AMR_B",
    "destination": "Wait_Buffer"
  },
  "manager_briefing": {
    "summary": "안벽 크레인 동선 간섭 우회 조치",
    "detail": "현시각 안벽 크레인 선석 구역(Ship_Dock)이 AMR_A호차에 의해 선점되어 있어, 동선 중첩 및 병목 방지를 위해 후속 AMR_B호차를 대기 구역(Wait_Buffer)으로 우회 조치했습니다."
  }
}
```

## 🔒 4. 결정론적 로컬 안전장치 명세 (0.01초 반사신경)

네트워크 오버헤드로 인한 n8n HTTP 통신의 지연 시간(0.5~1초)을 보완하기 위해, 노트북 내부 ROS2 토픽 레벨에서 독립적으로 작동하는 비상 제어 시나리오를 탑재한다.

- **인터럽트 발동 조건:** 탑다운 YOLO 세그멘테이션 주행로 구역 내에 `person`(사람/손) 감지 혹은 차량 전방 라이다 레이저 센서 기준 30cm 이내 돌발 장애물 인식 시.
- **제어 액션:** n8n의 명령을 강제로 뭉개고 즉시 차량 주행 노드의 `/cmd_vel` 파워 토픽을 `0.0`으로 고정 발행하여 급정거를 수행한다.
- **대뇌 고지:** 기기 완전 정지 완료 후 n8n 웹훅에 `{"event_type": "EMERGENCY_STOP"}`을 송출하여 서버 측 관제 타임라인 및 스케줄러를 일시 동결(Lockout) 시킨다.

## 🚢 5. 구현 타겟 항만 시나리오 3종

1. **얀-샤플 (Yard Shuffle) 미션:** 야적장에 적재된 목표 상자 위에 방해 상자가 쌓여 있는 것을 비전이 식별 ➡️ n8n 지시에 따라 로봇팔 크레인이 방해 상자를 옆 버퍼 구역으로 스무스하게 치워낸 후 하역 프로세스 재개.
2. **스토우지 플랜 (Stowage Plan) 미션:** 컨테이너 선적 우선순위(BOX_A 선출항 규칙) 데이터를 RAG에서 확인 ➡️ 교차로에서 일반 자재를 실은 차량 B를 일시 정지시키고 우선 자재를 실은 차량 A를 선박 쪽으로 먼저 통과시키는 동적 지능형 스케줄링.
3. **안벽 크레인 핸드오버 간섭 미션:** 단일 차선인 선박 하역 선석 구역의 점유 현황(YOLO 세그멘테이션)을 판독 ➡️ 선행 차량 작업 완료 시까지 후속 차량을 직전 진입 대기 선로(`Wait_Buffer`)에 완벽하게 격리 대기 제어.
